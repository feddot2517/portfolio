import React from "react";
import Menu from "../components/Menu";

const Mask = (props) => {
    return (
        <div>
            <Menu currentRoute="/masks"/>
        </div>
    )
}

export default Mask;